{{ if .Values.nexus.docker.enabled -}}
{{ $root := . }}
{{- range $registry := .Values.nexus.docker.registries }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "nexus.fullname" $root | trunc 35 }}-dkr-{{ $registry.repo | trunc 20 }}
  namespace: {{ default $root.Release.Namespace $root.Values.namespaces.nexusNs.name | quote }}
  labels:
    {{- include "nexus.labels" $root | nindent 4 }}
  annotations:
    haproxy.router.openshift.io/balance: "source"
    haproxy.router.openshift.io/timeout: "600s"
    haproxy.router.openshift.io/timeout-tunnel: "3600s"
    haproxy.router.openshift.io/disable_cookies: "true"
spec:
  # Hostname is now generated from the 'host' property with the new domain
  host: {{ $registry.host }}.nexus-t01.sunrise.ch
  to:
    kind: Service
    name: {{ include "nexus.fullname" $root | trunc 54 }}-docker
    weight: 100
  port:
    targetPort: docker-{{ $registry.port }}
  tls:
    termination: {{ default "edge" $root.Values.nexus.docker.tls.termination }}
    insecureEdgeTerminationPolicy: {{ default "Redirect" $root.Values.nexus.docker.tls.insecureEdgeTerminationPolicy }}
    {{- if $root.Values.nexus.docker.tls.certificate }}
    certificate: |
      {{ $root.Values.nexus.docker.tls.certificate | nindent 6 }}
    {{- end }}
    {{- if $root.Values.nexus.docker.tls.key }}
    key: |
      {{ $root.Values.nexus.docker.tls.key | nindent 6 }}
    {{- end }}
    {{- if $root.Values.nexus.docker.tls.caCertificate }}
    caCertificate: |
      {{ $root.Values.nexus.docker.tls.caCertificate | nindent 6 }}
    {{- end }}
---
{{- end }}
{{- end }}
